# 実機から mac 上の API へ届くためのベース URL（スマホと mac が同一 Wi-Fi であること）
# mac の LAN IP は「システム設定 > Wi-Fi > 詳細」または `ipconfig getifaddr en0` で確認
# 例: EXPO_PUBLIC_API_URL=http://192.168.1.20:18080
# シミュレータ/エミュレータのみで開発する場合は未設定（このファイルを .env にコピーしない）でよい
EXPO_PUBLIC_API_URL=
